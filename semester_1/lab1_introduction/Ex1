
#include <iostream>

int main()
{
    int n, m;
    std::cin >> n >> m;
    if (n <= 0 ||m <= 0){
   std::cout << "n,m must be positive!";
        return -1;
    }
    std::cout << 1 << " ";
        for (int i = 2; i <=std::min(n, m); i++) {
            if ((m % i == 0) && (n % i == 0)) {
           std::cout << i << " ";
            }
        }
        return 0;
    }
    

